[pytest]
testpaths = tests
addopts = -v --html=reports/report.html --self-contained-html
markers =
    smoke: Quick sanity checks — run first
    regression: Full regression suite
    e2e: End-to-end tests that hit a real email inbox (Guerrilla Mail)
